# vi: set ft=gnuplot :

if (!exists("input")) {
    print "No input file given"
    exit
}

if (!exists("type")) {
    type='Throughput'
}

set title font "Helvetica,20"
set key left top Left

set terminal eps

filename='sconekv-dist-comparision-'.type.'.eps'
set output filename

set style line 2 lc rgb 'black' lt 1 lw 1
set style data histogram
set style histogram cluster gap 1
set style fill pattern border -1
set boxwidth 0.9
set xtics format ""
set grid ytics

ylabel=type.' (ops/sec)'
set ylabel ylabel
set xlabel "Number of client threads"

title='SconeKV workload distribution comparision'
set title title
plot input using 2:xtic(1) title "Zipfian" ls 2, \
            '' using 3 title "Uniform" ls 2

